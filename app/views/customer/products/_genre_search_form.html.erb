<%= search_form_for(@q, url:genre_search_products_path) do |f| %>
  <%= f.collection_select :genre_id_eq, genres, :id, :name, {:include_blank => '指定なし'}, { class:"form-control-sm" } %>
  <%= f.submit '検索', class:"btn btn-sm btn-primary" %>
<% end %>